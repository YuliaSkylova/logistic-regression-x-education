# logistic-regression-x-education

# Proyecto de Regresión Logística

Este proyecto implementa un modelo de **regresión logística** para analizar y predecir variables binarias.  
La regresión logística es un modelo estadístico ampliamente utilizado cuando la variable dependiente solo puede tomar dos valores (por ejemplo: sí/no, éxito/fracaso, 1/0).

El objetivo del proyecto es:
- Comprender el funcionamiento de la regresión logística.
- Ajustar un modelo utilizando datos reales.
- Interpretar los coeficientes, odds ratios y significancia estadística.
- Evaluar la calidad del ajuste mediante métricas y pruebas estadísticas.
- Realizar predicciones y análisis de clasificación.

---

## 📂 Estructura del Proyecto

/logistic-regression-x-education/
│── data/
│   ├── Leads X Education.csv
│── src/
│   ├── logistic-regression-x-education.ipynb
│── README.md
│── requirements.txt

---

## Cómo ejecutar el proyecto

Este proyecto está diseñado para ejecutarse en **Jupyter Notebook** 
dentro de un entorno **Anaconda**.  
No es necesario ejecutar scripts `.py` desde la terminal.

---

### 1. Crear y activar el entorno (Anaconda)

Abre Anaconda Prompt y ejecuta:

```
conda create -n logistica python=3.10
conda activate logistica
```


## 2. Instalación de dependencias

Antes de ejecutar el proyecto, instala las librerías necesarias:

```
pip install -r requirements.txt
```
## 2. Abrir Jupyter Notebook

**Launch**

##3. Ejecuta celdas por orden para ver resultados o desde el menu de Jupyter 

**Run - Run All Cells**

